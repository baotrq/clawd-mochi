// Copy this file to secrets.h (same folder) and fill in real values.
// secrets.h is gitignored (see .gitignore at repo root) — never committed.
// Same values as clawd_mochi/secrets.h; duplicated here only because
// Arduino sketches can't #include a file from outside their own folder.

#pragma once

#define STA_SSID ""   // your 2.4 GHz WiFi network name
#define STA_PASS ""   // your WiFi password

#define FOOTBALL_DATA_API_KEY ""
#define BALLDONTLIE_API_KEY ""
#define RAPIDAPI_FLASHSCORE_KEY ""
